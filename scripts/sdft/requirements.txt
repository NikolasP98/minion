# SDFT Phase 1 - Python dependencies
# Install: pip install -r scripts/sdft/requirements.txt

# Database
libsql-experimental>=0.0.47

# PII scrubbing (M4 compliance)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0

# Teacher model API
anthropic>=0.39.0

# Fine-tuning (QLoRA - C2 compliance)
torch>=2.2.0
transformers>=4.44.0
peft>=0.13.0
datasets>=2.20.0
bitsandbytes>=0.43.0      # 4-bit NF4 quantization
accelerate>=0.33.0
scipy>=1.13.0

# Evaluation
tqdm>=4.66.0
