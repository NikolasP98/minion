apiVersion: v1
kind: Service
metadata:
  name: omniparser
  namespace: minion
  labels:
    app: omniparser
spec:
  type: ClusterIP
  selector:
    app: omniparser
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
